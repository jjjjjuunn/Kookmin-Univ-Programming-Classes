# 해시 테이블 완전 정복 🔥

## 목차
1. [해시 테이블 기본 개념](#1-해시-테이블-기본-개념)
2. [개방형 주소 방식 (Open Addressing)](#2-개방형-주소-방식-open-addressing)
3. [폐쇄 주소 방식 (Chaining)](#3-폐쇄-주소-방식-chaining)
4. [내가 헷갈렸던 질문들 Q&A](#4-내가-헷갈렸던-질문들-qa)
5. [완성 코드 모음](#5-완성-코드-모음)
6. [시험 대비 핵심 요약](#6-시험-대비-핵심-요약)

---

## 1. 해시 테이블 기본 개념

### 왜 해시 테이블인가?
- **배열/리스트 검색**: O(n) - 처음부터 끝까지 다 찾아봐야 함
- **해시 테이블**: O(1) - 해시 함수로 위치를 바로 계산!

### 해시 함수란?
```
h(key) = key % table_size
```
- 예: 13을 크기 7인 테이블에 저장
- `h(13) = 13 % 7 = 6` → 인덱스 6번에 저장

### 충돌(Collision)이란?
두 개 이상의 키가 같은 인덱스에 저장되려고 할 때!
- 예: 테이블 크기 7
  - `h(13) = 13 % 7 = 6`
  - `h(20) = 20 % 7 = 6` ← **충돌!**

---

## 2. 개방형 주소 방식 (Open Addressing)

### 핵심 아이디어
충돌이 나면 **다른 빈 슬롯을 찾아서** 저장!

### 선형 조사법 (Linear Probing)
```python
h(i, key) = (key + i) % table_size
```
- i = 0, 1, 2, ... 순서대로 시도
- 빈 자리가 나올 때까지 한 칸씩 이동

### 예시: 크기 7 테이블에 13, 20, 27 삽입
```
초기 상태: [None, None, None, None, None, None, None]

1) 13 삽입
   h(0, 13) = 13 % 7 = 6
   → [None, None, None, None, None, None, 13]

2) 20 삽입
   h(0, 20) = 20 % 7 = 6 → 충돌! (13 있음)
   h(1, 20) = 21 % 7 = 0 → 빈자리!
   → [20, None, None, None, None, None, 13]

3) 27 삽입
   h(0, 27) = 27 % 7 = 6 → 충돌! (13 있음)
   h(1, 27) = 28 % 7 = 0 → 충돌! (20 있음)
   h(2, 27) = 29 % 7 = 1 → 빈자리!
   → [20, 27, None, None, None, None, 13]
```

### DELETED 마커가 필요한 이유

**문제 상황:**
```
테이블: [20, 27, None, None, None, None, 13]
20을 삭제 → [None, 27, None, None, None, None, 13]

이제 27을 검색하면?
h(0, 27) = 6번 확인 → 13 있음
h(1, 27) = 0번 확인 → None 발견! 
→ "27 없음" 이라고 잘못 판단! ❌
```

**해결책: DELETED 마커**
```
20 삭제 → [DELETED, 27, None, None, None, None, 13]

27 검색:
h(0, 27) = 6번 확인 → 13 (계속 탐색)
h(1, 27) = 0번 확인 → DELETED (계속 탐색!) ✅
h(2, 27) = 1번 확인 → 27 발견!
```

### 핵심 포인트
- **삽입**: None 또는 DELETED 자리에 가능
- **검색**: DELETED는 건너뛰고 계속 탐색
- **삭제**: 값을 DELETED로 교체

---

## 3. 폐쇄 주소 방식 (Chaining)

### 핵심 아이디어
충돌이 나면 **같은 인덱스에 연결리스트로 연결**해서 저장!

### 구조
```
table[0] → [20] → None
table[1] → [27] → [8] → None
table[2] → None
table[3] → [10] → None
table[4] → None
table[5] → None
table[6] → [13] → [34] → [55] → None
```

### 왜 무한정 입력이 가능한가?
**내 질문:** "개방형은 테이블 크기만큼만 저장되는데, 폐쇄형은 왜 무한정 가능?"

**답:**
1. 각 슬롯마다 **연결리스트**로 무제한 연결 가능
2. **부하율(Load Factor)** 관리
   ```
   load_factor = 저장된 데이터 수 / 테이블 크기
   ```
3. load_factor ≥ 1.5 되면 **테이블 크기를 2배로 확장**하고 **재해싱(Rehashing)**

### 재해싱 과정
```python
# 1. 새 테이블 생성 (크기 2배)
new_table = [None] * (old_size * 2)

# 2. 기존 데이터를 모두 새 테이블에 재삽입
for slot in old_table:
    node = slot
    while node:
        new_index = node.data % new_size
        # new_table[new_index]에 연결
        node = node.link
```

### SNode 구조
```python
class SNode:
    def __init__(self, data, link):
        self.data = data  # 데이터 값
        self.link = link  # 다음 노드 참조
```

### 삽입 과정
```python
def insert(self, num):
    slot = num % len(self.__table)
    
    # 1. 중복 검사
    p = self.__table[slot]
    while p:
        if p.data == num:
            return False  # 이미 존재
        p = p.link
    
    # 2. 맨 앞에 삽입
    new_node = SNode(num, self.__table[slot])
    self.__table[slot] = new_node
    self.__numItem += 1
    
    # 3. 부하율 확인 및 재해싱
    if self.__numItem / len(self.__table) >= 1.5:
        self.__resizing()
```

---

## 4. 내가 헷갈렸던 질문들 Q&A

### Q1: `\` 백슬래시는 뭐야?
**A:** 줄바꿈 연속 기호! 긴 코드를 여러 줄로 나눠 쓸 때 사용

```python
# 이렇게 쓰면 한 줄이 너무 길어서 읽기 힘듦
if self.__table[index] != None and self.__table[index] != self.__DELETED and self.__table[index] == num:

# 백슬래시로 나눠쓰기
if self.__table[index] != None and \
   self.__table[index] != self.__DELETED and \
   self.__table[index] == num:
```

### Q2: index 출력 버그
```python
# ❌ 잘못된 코드
index = self.search(num)
print(f'index: {self.__table[index]}')  # 13 출력 (값!)

# ✅ 올바른 코드  
print(f'index: {index}')  # 0 출력 (인덱스!)
```

### Q3: `h._Chaining__table`은 뭐야?
**A:** Python의 **Name Mangling** (이름 변형)

```python
class Chaining:
    def __init__(self):
        self.__table = [None] * 7  # private 변수

# 클래스 외부에서 접근
h = Chaining()
print(h.__table)  # ❌ AttributeError
print(h._Chaining__table)  # ✅ 접근 가능!
```

**패턴:** `_ClassName__variableName`

### Q4: BST에서 `self.__root = _insertBST(...)`는 왜?
**A:** Python은 **참조를 값으로 전달**하기 때문!

```python
# 잘못된 이해
def _insert(node, num):
    if node is None:
        node = TreeNode(num)  # ❌ 지역변수만 바뀜!
    return node

# 올바른 사용
def _insert(node, num):
    if node is None:
        return TreeNode(num)  # ✅ 새 노드 반환
    # ... 재귀 호출 ...
    return node

# 외부에서 반환값 할당 필수!
self.__root = _insert(self.__root, num)
```

### Q5: 폐쇄형은 왜 global head가 없어?
**A:** 각 슬롯이 **독립적인 연결리스트**의 head!

```python
# 개방형/일반 연결리스트: 전역 head 하나
__head = None

# 폐쇄형: 슬롯마다 head
__table = [None] * 7
# table[0]이 첫 번째 리스트의 head
# table[1]이 두 번째 리스트의 head
# ...
```

---

## 5. 완성 코드 모음

### 개방형 주소 방식 (Linear Probing)

```python
class HashOpenAddr:
    def __init__(self, n):
        self.__table = [None] * n
        self.__numItem = 0
        self.__DELETED = -54321  # DELETED 마커
    
    def __Hash(self, i, num):
        return (num + i) % len(self.__table)
    
    def insert(self, num):
        # 중복 확인
        if self.search(num) != None:
            return False
        
        # 빈 슬롯 찾기
        slot = None
        i = 0
        while i < len(self.__table):
            index = self.__Hash(i, num)
            if self.__table[index] == None or \
               self.__table[index] == self.__DELETED:
                slot = index
                break
            i += 1
        
        if slot == None:
            return False  # 테이블 가득참
        
        self.__table[slot] = num
        self.__numItem += 1
        return True
    
    def search(self, num):
        i = 0
        while i < len(self.__table):
            index = self.__Hash(i, num)
            
            if self.__table[index] == None:
                return None  # 검색 실패
            
            if self.__table[index] == num:
                return index  # 검색 성공
            
            i += 1
        
        return None  # 테이블 전체 탐색 실패
    
    def delete(self, num):
        index = self.search(num)
        if index == None:
            return False
        
        self.__table[index] = self.__DELETED
        self.__numItem -= 1
        return True
    
    def printTable(self):
        for i, val in enumerate(self.__table):
            print(f'table[{i}] = {val}')
```

### 폐쇄 주소 방식 (Chaining)

```python
class SNode:
    def __init__(self, data, link):
        self.data = data
        self.link = link

class Chaining:
    def __init__(self, n):
        self.__table = [None] * n
        self.__numItem = 0
    
    def insert(self, num):
        slot = num % len(self.__table)
        
        # 중복 검사
        p = self.__table[slot]
        while p:
            if p.data == num:
                return False
            p = p.link
        
        # 맨 앞에 삽입
        new_node = SNode(num, self.__table[slot])
        self.__table[slot] = new_node
        self.__numItem += 1
        
        # 부하율 확인
        if self.__numItem / len(self.__table) >= 1.5:
            self.__resizing()
        
        return True
    
    def search(self, num):
        slot = num % len(self.__table)
        p = self.__table[slot]
        
        while p:
            if p.data == num:
                return slot
            p = p.link
        
        return None
    
    def delete(self, num):
        slot = num % len(self.__table)
        p = self.__table[slot]
        
        # 첫 번째 노드 삭제
        if p and p.data == num:
            self.__table[slot] = p.link
            self.__numItem -= 1
            return True
        
        # 중간/끝 노드 삭제
        while p and p.link:
            if p.link.data == num:
                p.link = p.link.link
                self.__numItem -= 1
                return True
            p = p.link
        
        return False
    
    def __resizing(self):
        old_table = self.__table
        self.__table = [None] * (len(old_table) * 2)
        self.__numItem = 0
        
        # 재해싱
        for slot in old_table:
            p = slot
            while p:
                self.insert(p.data)
                p = p.link
    
    def printTable(self):
        for i in range(len(self.__table)):
            print(f'table[{i}]: ', end='')
            p = self.__table[i]
            while p:
                print(f'{p.data} → ', end='')
                p = p.link
            print('None')
```

---

## 6. 시험 대비 핵심 요약

### 시간 복잡도 비교

| 연산 | 개방형 (최선) | 개방형 (최악) | 폐쇄형 (최선) | 폐쇄형 (최악) |
|------|--------------|--------------|--------------|--------------|
| 삽입 | O(1) | O(n) | O(1) | O(n) |
| 검색 | O(1) | O(n) | O(1) | O(n) |
| 삭제 | O(1) | O(n) | O(1) | O(n) |

### 장단점 비교

**개방형 주소 방식**
- ✅ 장점: 메모리 효율적 (추가 포인터 불필요)
- ❌ 단점: 
  - 테이블 크기 제한 있음
  - 클러스터링 (군집화) 발생 가능
  - 삭제 시 DELETED 마커 필요

**폐쇄 주소 방식**
- ✅ 장점:
  - 무제한 데이터 저장 (연결리스트)
  - 삭제 간단
  - 클러스터링 문제 없음
- ❌ 단점:
  - 포인터 저장 공간 필요
  - 캐시 효율 낮음

### 시험 단골 문제

**1. 해시 함수 계산**
```
테이블 크기 7에 13, 20, 27 삽입
h(k) = k % 7
→ 각각 어느 인덱스에 저장?
```

**2. 충돌 해결 과정 추적**
```
선형 조사법으로 13, 20, 27 삽입 과정 단계별로 그리기
```

**3. DELETED 마커 필요 이유**
```
왜 None으로만 삭제하면 안 되는가?
```

**4. 부하율 계산**
```
load_factor = 데이터 수 / 테이블 크기
언제 재해싱이 필요한가?
```

**5. 시간 복잡도 분석**
```
최선/평균/최악의 경우 각각 설명
```

---

## 마무리

### 핵심 체크리스트
- [ ] 해시 함수 계산 (h(k) = k % size)
- [ ] 선형 조사법 충돌 해결 과정
- [ ] DELETED 마커의 역할과 필요성
- [ ] Chaining 삽입/검색/삭제 과정
- [ ] 부하율과 재해싱 개념
- [ ] Python Name Mangling (`_ClassName__variable`)
- [ ] 개방형 vs 폐쇄형 장단점

### 공부 팁
1. **손으로 직접 그리기**: 테이블 상태 변화를 단계별로 그려보기
2. **코드 변형 연습**: 이중 해싱, 이차 조사법 등 다른 충돌 해결 방법 구현
3. **시간 복잡도 분석**: 각 연산이 왜 그런 복잡도를 갖는지 이해
4. **디버깅 연습**: 의도적으로 버그 만들고 고쳐보기

화이팅! 🔥
